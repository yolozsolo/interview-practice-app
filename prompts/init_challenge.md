Use prompts/create_challenge.md.

Create a new valid Python challenge file under challenges/.

Topic focus:
Python typing, data normalization, deduplication, edge cases, and mapping the solution to PySpark DataFrame operations.

Challenge type:
mixed

The file must compile with:
uv run python <file>

It should also include pytest-style tests that can run with:
uv run pytest <file>

Do not solve the challenge.